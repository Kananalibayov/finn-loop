# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# env
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# zcode
# Skills (.zcode/skills/) are tracked; plans/ are per-session scratch.
.zcode/plans/

# AC-7 (issue #4): local SQLite database + data dir — runtime only, never commit.
/data

# Builder scratch files (PR/issue body drafts written to disk to avoid
# shell-escaping; must never be committed).
.pr-*.md
.issue-*.md
